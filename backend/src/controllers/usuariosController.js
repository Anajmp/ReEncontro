// =====================================================================
// usuariosController — camada HTTP do módulo usuarios.
// TODO: implementar. Use itensController.js como molde.
// =====================================================================
export const usuariosController = {
  // exemplo:
  // async listar(req, res, next) {
  //   try { ... } catch (err) { next(err); }
  // },
};
